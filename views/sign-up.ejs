<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="styles.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto+Mono:wght@500;700&display=swap"
      rel="stylesheet"
    />
    <link rel = "stylesheet" href = '/styles.css'/>
    <title>Sign Up</title>
  </head>
  <body>
    <div class="signup main">
      <div class="signup container">
        <div class="signup heading"><h1>Sign up</h1></div>
        <form action="" method="POST">
          <div class="username form-item">
            <label for="username">Username</label>
            <input type="text" id="username" name="username" required>
          </div>
          <div class="password form-item">
            <label for="password">Password</label>
            <input type="password" id="password" name="password" minlength = 8 title = "Password must be of 8 characters with 1 lowercase, 1 uppercase, 1 number and 1 special symbols" required />
          </div>
          <div class="conf-password form-item">
            <label for="confirm">Confirm Password</label>
            <input type="password" id="confirm" name="confirm" required />
          </div>
          <button type="submit">Submit</button>
        </form>
        <% if(typeof errors !== 'undefined') {%>
          <ul>
          <% errors.forEach(function(error){ %>
          <li><%= error.msg %></li>
          <% }) %>
        </ul>
       <% } %>   
      </div>
    </div>
  </body>
</html>
